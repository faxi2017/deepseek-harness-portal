# 项目全局协作要求

## 代码发布流程

每次代码改动都必须遵循以下流程：

1. 在本地完成相应验证。
2. 在本地提交 Git commit 并 push 到远程仓库。
3. 服务器在项目目录执行 Git pull 获取该提交后再部署或重启服务。
4. 部署后验证服务与本次改动的实际结果。

禁止通过 SCP、rsync 或直接覆盖服务器源码作为发布方式。服务器不应产生仅存在于本机、未提交到远程仓库的代码改动。
