# 默认插件配置与批量安装

管理员登录 Portal，打开左侧“默认插件”。每行输入一个命令，例如：

```sh
dsh plugin --profile web add dshmarket
```

官方 [dsh-market 安装说明](https://github.com/dsh-market/dsh-market) 要求安装后重启 DSH。当前 npm DSH 的 `plugin add` 会调用 pnpm 安装依赖，并把声明 `dsh.bundle.patch` 的包加入 `web` profile 的 bundles。Portal 复用这个机制，不修改 DSH 源码或为用户生成不同镜像。

## 后台操作

1. **保存默认插件**：新用户创建实例时自动安装、启用并重启。每行一个 npm 插件包；支持 `@组织/包名`、`包名@1.2.3` 和版本标签。不支持 shell 命令、路径、Git 地址、其他 profile 或附加参数。
2. **为全部已有实例安装并重启**：执行已保存的命令，逐个处理实例；安装成功后重启并检查 HTTP 健康。停止的实例会启动；重启会中断当前对话连接。
3. **安装 / 重试**：仅处理这一实例。重复提交正在执行的任务不会重复安装。单个失败不阻止队列中其他实例；任务不依赖页面保持打开。
4. 状态展示排队、执行、成功或失败，以及成功任务检查到的包版本。安装期间不能修改默认配置；列表每 6 秒刷新，不覆盖正在编辑的文本框。
5. 移除命令或清空后保存，只改变默认安装，不自动卸载任何已有插件。用户仍可自行管理其他插件。

未填写版本的命令在每次实际执行时由 npm 解析版本，因此“安装 / 重试”也可能更新该插件。若需要固定版本，填写 `dsh plugin --profile web add dshmarket@1.41.0`。本地本次实际安装版本为 **1.41.0**。

## 生命周期和失败处理

- 插件、profile 与依赖位于 `/home/dsh/.dsh/profiles/web`，属于用户独立的 home 卷。工作区仍使用原 workspace 卷；不把插件安装进只读镜像。
- 创建与重建实例时读取默认配置。已经成功执行当前配置的实例在重建时保留卷，不重复安装或自动更新；管理员更改默认配置后，下一次重建会应用新配置，也可立即手动批量下发。正常启动不强制重装用户自行调整的插件。
- 安装、停止、删除、重建复用同一实例生命周期锁；空闲回收跳过正在安装的实例。安装以 UID 1000 运行，保留现有网络及资源限制，不授予 root、Docker socket 或额外挂载。
- 命令经严格解析后以参数数组执行，不经过 shell；单个安装在容器内最多运行 300 秒，随后终止，10 秒后仍未退出则强制结束。宿主机有更长的外层超时；容器内文件锁避免 Portal 重启后与仍在结束的安装重叠。
- 命令成功后检查依赖存在、包声明插件 bundle、已加入 profile，再重启并检查健康。普通 npm 库不会被误报为已启用插件。健康检查不能认证任意插件的全部功能或未来 DSH 版本兼容性。
- 安装失败不自动重启，也不自动回滚已安装的部分内容；提示不包含命令输出或服务端内部信息。修复网络、包名或兼容性后单独重试。重启后健康检查失败会同时标记实例失败。
- Portal 重启将未完成任务标为失败，要求手动重试，避免重复执行结果未知的安装。原有未完成实例创建仍按既有机制重新编排。
- 只支持现有单 Portal 进程部署。需要 root、额外系统库或手动批准构建脚本的插件，不在此输入框内自动处理；当前 `dshmarket` 在现有镜像权限下已验证成功。
- 安装流程不写模型、凭证或默认模型配置。插件代码本身在用户容器内运行，具体插件能力和 DSH 升级兼容性仍需逐项验证。

## 接口与存储

所有接口复用管理员登录、角色、Host/Origin 和 CSRF 校验。

| 接口 | 用途 |
| --- | --- |
| `GET /api/admin/plugins` | 默认命令、配置版本、实例执行状态 |
| `POST /api/admin/plugins` | `{commands: "每行一条安装命令"}`，保存默认值 |
| `POST /api/admin/plugins/apply` | `{}` 批量下发；`{instanceId: 123}` 单实例安装；返回 202 |

`settings.plugin_defaults` 保存默认命令及递增版本；`instance_plugins` 保存各实例最后一次任务的命令快照、版本、状态、检查到的插件版本和脱敏结果。与 Portal SQLite 一起备份，插件本体与用户 home 卷一起备份。

## 本地验收（2026-09-03）

```sh
npm test --prefix portal
node --env-file=.env scripts/smoke-plugins.mjs
```

第二条需要已配置默认插件和可用 Docker，会下载插件、创建独立测试用户和容器，验证后仅清理本次测试资源，不调用模型。

- 42 项自动化测试通过，包含原有网关、注册、会话、代理和容器回归，以及命令校验、重复任务、失败重试、删除竞态、停止操作串行化和中断恢复。
- 后台输入指定命令并批量下发，现有 `fangxi` 安装 `dshmarket@1.41.0`，重启健康；模型配置及默认选择哈希与安装前一致。
- 通过真实注册接口创建用户，默认插件和平台模型自动配置成功；浏览器在原生 DSH 设置中打开“插件市场”，目录加载成功。
- 真实 Docker 冒烟验证重复安装、自动重启、保留卷重建，确认个人模型、默认选择、工作区和插件均保留。

以上为本地验证，服务器尚未部署本次功能。
